using System;
using System.Collections;

namespace dbs.ObjectOrientatedProgramming.ClassNotes.CollectionsAndLists
{
    class SortedListClass
    {
        //Add to Main()
        //////"SortedList" - Represents a collection of key/value pairs
        //////that are sorted by the keys and are accessible by key
        /////// and by index

        //SortedList people = new SortedList(); //Note no data type

        ////add based on a key (in this e.g student id)
        //people.Add(170000, "John Jones");
        //people.Add(143243243, "Pat Peterson");

        ////to remove an element you must use the key
        ////people.Remove(170000)

        ////people.Remove(170000);
        ////When you use foreach to enumerate the elements in a 
        ////Collection, the elements are retrieved as Dictionary
        ////Objects

        ////What are Dictoionary entry objects?

        //foreach (DictionaryEntry entry in people)
        //{
        //    Console.Write("Key:{0}", entry.Key);
        //    Console.WriteLine(" Value:{0}", entry.Value);
        //    Console.WriteLine();
        //}
}
}
